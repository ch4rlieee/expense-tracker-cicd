{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Simple Expense Tracker with JSON file storage",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"No unit tests configured\" && exit 0",
    "lint": "eslint app.js || true"
  },
  "keywords": ["expense", "tracker", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.50.0"
  }
}
